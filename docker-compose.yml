services:
  prism:
    build: .
    image: prism:dev
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - prism-data:/app/data
    command: uvicorn prism.api.app:create_app --factory --host 0.0.0.0 --port 8000 --reload

volumes:
  prism-data:
